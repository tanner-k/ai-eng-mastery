[project]
name = "ai-eng-mastery"
version = "0.1.0"
description = "Build-it-yourself AI Engineering curriculum in PyTorch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.2",
    "numpy>=1.26",
    "matplotlib>=3.8",
    "jupyterlab>=4.0",
    "pytest>=8.0",
    "ruff>=0.5",
    "scikit-learn>=1.9.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
# Discover tests anywhere in the repo so new section exemplars are picked up
# automatically (pytest skips .venv/.git via default norecursedirs).
testpaths = ["."]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint.per-file-ignores]
# Notebooks legitimately need path/setup code before imports (E402), use
# single-letter math names (I for identity, etc.) that trip E741, and define
# dataclasses in later cells with `from __future__ import annotations` (F404 is a
# false positive across concatenated cells).
"*.ipynb" = ["E402", "E741", "F404"]
